@model IEnumerable<EQS.Models.LogSet>
@{ ViewBag.Title = "Журнал"; }
<h2>Журнал работы очереди</h2>

<table class="table">
    <tr>
        <th>Время</th>
        <th>Клиент</th>
        <th>Операция</th>
        <th>Сложность</th>
        <th>Событие</th>
        <th>Окно</th>
        <th></th>
    </tr>
@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Client)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Operation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TimeNeeded)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Event)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Window)
        </td>
    </tr>
}
</table>
